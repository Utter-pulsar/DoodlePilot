# Electron downloads its prebuilt binary from this mirror; switch to the default if you
# are outside mainland China. Keeps `pnpm install` / `npm install` fast and reliable.
electron_mirror=https://npmmirror.com/mirrors/electron/
# Ensures native deps are built against Electron's ABI, not system Node.
runtime=electron
